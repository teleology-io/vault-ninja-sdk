{
  "name": "@teleology/vn",
  "version": "1.0.3",
  "description": "Vault Ninja SDK — read org secrets programmatically",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "build": "tsc",
    "harness": "tsc && node dist/harness.js"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
